// Attach copy that browser code and tests also read, kept apart from the
// server-only attach routes.

/** Sends the owner to Manage's "Update connection service" button, by its name. */
export const ATTACH_UPDATE_RUNTIME_COPY = "This computer's Hivra service is older than Codex needs. In Manage, choose Update connection service, then add Codex.";
