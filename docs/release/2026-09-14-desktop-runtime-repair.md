# Desktop runtime repair — 14 September 2026

## 22:47 continuation — local cursor and controller recovery

### Remaining-priority verification

- Omarchy reopened normally in displayed5.4seconds at the current release. Native Chrome switched to its blank tab and back; the same session remained connected, with no Prepare/Retry. The first post-return click measured75ms browser input→changed-frame with zero recorded stalls. This is a short real tab-return check, not a sustained performance benchmark or a new-machine launch.
- Cursor-shape matching was deliberately not added: the live pinned Selkies frontend exposes PNG image/dimensions/hotspots/handle, not semantic cursor names. The native local pointer remains the default; guessing images or expanding the protocol is outside this optional polish pass.
- A test-only JSDOM race was repaired by waiting for the actual transport-close binding before asserting cleanup;34broker tests and45capability/preparation tests pass with fresh review. Production cursor bytes and pins did not change.
- Existing focused recovery/integration checks passed:11Jest suites/150tests plus the real isolated PGlite folder-recovery migration harness. Actual guest Python covered binary-byte recovery and interruption cleanup. These are local checks, not live tenant/guest-folder/snapshot restoration or attached-agent acceptance. Local fixtures were cleaned; no VM or capacity was created.
- Fresh Windows remains unaccepted: the catalog is prepared-evaluation-guest-only, with no selected licensed image. Hetzner's published Cloud FAQ rejects Windows10/11 server use; its dedicated-server WindowsServer2025 SPLA option is a different product and requires an approved hosting/access license arrangement. The owner must choose Windows11 on approved infrastructure versus WindowsServer on Hetzner before a legitimate managed fresh-machine pipeline can be selected. No licenses were purchased or terms accepted.

- Omarchy's remaining delayed pointer was baked into the primary framebuffer, not an extra browser pointer. Installed Aquamarine 0.14.0-2 omitted the virtual DRM atomic cursor-hotspot capability. Reviewed recipe `119c0c503` requests that capability and commits the exposed hotspot properties. Three focused recipe tests and the actual guest C++ package build passed.
- VM2099 now has package `aquamarine-0.14.0-2.1` (archive SHA256 `87619dc2f86d67f2ea3608af965f786a9b3020563346eed1da36febbe56f42b1`). The stock 0.14.0-2 package was preserved from the Arch archive before installation. Normal owner Manage → Restart completed; DRM cursor plane36 now has a real Hyprland framebuffer rather than fb0. Normal Desktop reopened in displayed11.3seconds without Prepare/Retry. Moving the remote pointer, then moving the physical local pointer outside the desktop, left no baked pointer in the streamed wallpaper. One trusted click measured68ms browser input→changed-frame, not a physical latency benchmark.
- The user's normal local OS cursor is now the default, rather than Selkies' small guest-shaped CSS image. Omarchy-only policy `48b3ccd08` is live on Canary `dpl_94oPjE4VYY6paHswjvaQAGe8vvrq`;34broker and46capability/preparation tests passed, including reactive inline-important replacement and observer cleanup, plus fresh review. Normal Desktop automatically converged the update and opened in45.4seconds including installation. Native Chrome movement showed the same local pointer inside/outside the stream, with no trailing video pointer after leaving. One click measured35ms browser input→changed-frame (not sustained/physical latency proof). The test controller was released; VM remains running. No settings panel was added. The native package survives reboot, but fresh-image integration and future upstream upgrade policy are not yet accepted.
- Windows-native Terminal/Files guidance `133709173` is live on Canary `dpl_BPgmnaZG1JarvS7964L5dYkqKRLt`, verified through both tabs and their Open Desktop action. This is guidance to PowerShell/File Explorer in the actual Windows desktop, not a dedicated web PTY or web file-transfer implementation.
- Private local-controller backup/restore/key-rotation rehearsal passed in8minutes39seconds at `133709173`, with exact restored database markers and131073uploaded binary bytes, operator login, loopback isolation, key rewrap and retired-key rejection. Owned installations/containers/volumes/networks and encrypted backups were cleaned up; unrelated local fixture remained healthy. Receipt: `/tmp/hivra-private-controller-recovery.fkBM1E/recovery-e2e.json`. This does not establish public-source release, guest-folder restore, snapshots or fresh licensed Windows provisioning. No capacity was purchased.

Status: **All three retained computers pass normal Stop → Start → actual desktop opening without Prepare/Retry. Omarchy graphics, boot ordering, lifecycle lock, hidden startup and cursor repairs pass live checks; Windows keyboard/internet passes; Ubuntu no longer reinstalls its runtime after boot**. Eighteen-minute renewal continuity passes; forty-minute/network-interruption durability remains unverified. Existing desktops and user data are the targets; no VM rebuild or capacity purchase.

## Live release

- Branch: `codex/desktop-runtime-repair-20260914`; PR #616 remains open.
- Canary release: `02eea86f5`, deployment `dpl_8QbvyrPPspxaSoQUS5m4xN1TPKcx` (ready and aliased to `canary.hermesos.cloud`; immutable URL `hermesos-canary-9lhcf4yz2-ashneil12s-projects.vercel.app`). This is the Canary Vercel project, not the public production project. PR #616 is still open; preserve this release branch when continuing work rather than deploying an older checkout. Remote compilation and full TypeScript completed; live noindex headers and robots Disallow remain intact.
- Omarchy 2099 and Ubuntu 1108 have broker SHA256 `3c8814c5662fafa313a1cb6371ce34a5e313c0696d57fc9a3d830243e1ed585c`, verified at their respective `/usr/local/libexec/hivra/broker.cjs` and `/opt/hivra/remote-desktop/broker.cjs` paths. Supported updates completed and normal streams reopened at displayed 6.2 seconds (Omarchy) and 7.2 seconds (Ubuntu warm reopen). Ubuntu retains DPI `96` and locked CSS scaling. The Ubuntu opening including runtime repair took 83.4 seconds, not 7.2 seconds.

## Confirmed repairs

1. Lifecycle blocks are classified before guest preparation. Refresh quotas are isolated per computer; preparation limits remain intact.
2. Capability lifetime is eight minutes anchored to the actual observation, within the database's ten-minute maximum. Runtime source pins match deterministic shipped bytes, not unknown live drift.
3. Modern Selkies canvas readiness is recognized with positive finite numeric FPS and an authenticated open transport. Static/invalid canvas evidence is rejected. No fabricated canvas latency measurement.
4. The viewport uses the selected quality pixel budget with the panel's aspect ratio. The Omarchy consumer surface is embedded Selkies only; no Moonlight tab or VNC fallback. Guest Sunshine was not uninstalled.
5. Ubuntu's supported update path resynchronizes the current managed bundle before resuming a missing-receipt preparation. Failed synchronization retains the operation lock.

## Actual acceptance and preservation

- Omarchy's stale `desired_state=deleted` was restored with an exact conditional update only after confirming its running VM and idle operation slot. Its supported runtime convergence subsequently cleared preparation normally.
- Ubuntu's existing stopped VM 1108 was started, not recreated. Owner UI Update Desktop completed; a live screenshot shows its desktop filling the panel. Secure setup displayed 4.6 seconds; all three primary targets subsequently had null operation slots.
- Windows 2098 initially authenticated RDP but competing console-session arbitration disconnected it after approximately 32 seconds. Disconnecting console session 1 (not logging it off) allowed the browser RDP desktop to render; Edge loaded a Bing search. Conditional disabling of Hivra's console autologin preserves its credentials. A later QGA hang required a graceful reboot, preceded by the completed RAM-and-disk snapshot `hivra-before-qga-recovery-20260914`. The snapshot remains available; restoration has not been tested. QGA responds after that reboot; no service crash evidence established the cause of its earlier hang.
- Windows then failed capability freshness because QEMU supplied Berlin local time while Windows interpreted it as UK local time, leaving UTC one hour ahead. Persisted `localtime: 0` and `RealTimeIsUniversal: 1` plus trusted host UTC synchronization repaired observation and boot timestamps, without weakening guards or changing the user's timezone. The authentic inspector passes. The retained protected preparation subsequently completed through the normal owner button, and a live browser screenshot shows the RDP desktop after handoff. Mouse input opens Windows Search; keyboard interaction remains unconfirmed because browser automation does not reliably deliver it.
- Ubuntu's optional supported Manage runtime → Update runtime completed. Live `/var/lib/hivra/remote-desktop/selkies.env` and the running container parser both prove DPI `96` and locked CSS scaling enabled. Post-update secure setup displayed 4.1 seconds.
- Omarchy has the validated Lua-compatible installer `736c2930e641ccd3a2ad8e8408b6f6a31721ecdd1e62aaf9eba3f4de96d5c645`; its normal warm reopen displayed 5.1 seconds versus the earlier 19.5 seconds. Profile switching still exposes a separate capture-manager ownership conflict described below.

## Diagnosis history — final acceptance below supersedes rollout-pending entries

- Omarchy warm opening improved from 19.5 to 5.1 seconds after owner-scoped geometry control, read-only `/run/user` visibility, Lua-compatible Hyprland monitor rules, and official CSS scaling. Changing profiles exposed another root cause: an active Selkies wlr-output-management controller overrides Lua monitor rules. QHD requests the correct approximately 3.68MP budget, but the old preflight fights that controller and leaves the previous large geometry. Reviewed commit `5b4c7d7e4` coordinates active capture ownership and verifies realized capture plus host geometry before publishing. Seven Python and 37 Jest tests pass; deployment and live profile acceptance are in progress. Initial client settings may still request DPR-scaled geometry before the subsequent correct quality message; this separate boot inefficiency is not yet resolved.
- The Windows operation `00000000-0000-4000-8000-000000001152` was resumed through owner preparation after repairing QGA and UTC; no fabricated receipt or lease release was used. The earlier QGA hang's underlying cause remains unproven.
- Windows preparation classification repair `d92c5946d` is committed, pushed and included in the live release above, with 38 focused tests, ESLint and a fresh source review passing. Only confirmed RDP configuration faults allow automatic preparation. Transport/SSH/QGA/receipt failures remain read-only. The live private maintenance ports 22/5985/5986/445/135 are unavailable; 3389 responds. The reboot used for QGA recovery was preceded by a completed preservation snapshot, not forced through with unsaved state unprotected.
- Ubuntu's normal reopen also rendered without preparation, but secure setup was 11.1 seconds in an earlier check (not always 4.6). Live parser inspection confirmed its old `96|locked` DPI enum invalid and CSS scaling disabled. `030c08503` replaces the enum with `96` and adds official `true|locked` CSS scaling; deterministic bundle `e65e6bbc9e03d316db1de39a70181d658e2c5380f3cd1c0c92a6d8737a2c83ae`, 94 focused tests pass. The supported live rollout and running parser are now verified.
- Built-in browser snapshots work, but keyboard/mouse actions intermittently report an unavailable focus root and can partially deliver input. Chrome is unavailable. Do not interpret attempted input or the connection badge alone as full interaction acceptance.
- Duplicate cursor, sustained responsiveness, background-return recovery, new-VM first boot, and fleet-wide launch acceptance are not yet confirmed. Cursor source inspection proves separate browser/video rendering policies, but did not reproduce an actual duplicate-rendering defect; no speculative cursor changes were made.
- Extra Ubuntu VMs 1109 (`00000000-0000-4000-8000-000000001153`) and 1112 (`00000000-0000-4000-8000-000000001154`) had idle operation slots and no desktop sessions. Both were shut down through their authenticated owner Stop buttons. UI, Proxmox power state, and database desired/status all confirm stopped with null operations. Files were not deleted; Ubuntu 1108 remains running alongside Windows 2098 and Omarchy 2099.
- `5b4c7d7e4` did not pass live acceptance: native QHD resize timed out and a subsequent fresh reconnect failed retained-controller preflight. Reviewed restoration `c4c2432a8` uses retained-controller ownership correctly, reconciles refused geometry without broadcasting false dimensions, and normalizes the proven initial dynamic DPR request. Seven Python and 38 Jest checks pass. Its installer `1159022ece34df54d16cf8eae70a1015cd1a390af067f8f4b00c70440e376d6c` is installed; fresh QHD opening renders again, but native profile changes still timed out.
- The first compositor-refresh diagnostic preceded native submission and was inconclusive. A corrected single probe triggered on native `HostCapture waiting`, refreshed the compositor after 250ms, and observed actual mode change from 2752×1338 to 2064×1004 within 300ms. The decoded browser canvas independently matches 2064×1004. This establishes queued compositor frame progression as the blocker. A persistent, scoped frame-cadence policy is being implemented; no claim of unassisted native profile acceptance yet.
- The reviewed cadence policy captures the existing boolean `debug:vfr` on the exact validated Hyprland instance, applies fixed `hl.config({debug={vfr=false}})` with an initial compositor refresh, and verifies the actual option before serving the private layout socket. It restores the previous boolean on normal shutdown/SIGTERM without editing user configuration. Eight Python and 38 Jest checks pass. Restoration cannot be guaranteed if the compositor is unavailable during shutdown; live rollout remains pending.
- Ubuntu's decoded canvas changes from QHD 2160×1696 (about 3.66MP) to HQ 1616×1264 (about 2.04MP) with the actual tall panel aspect ratio. Screenshots show the desktop filling that panel. Windows Edge opens through mouse input and loads its online MSN page after the clock repair; automated keyboard delivery remains unconfirmed.

## Verification boundary

Focused tests and reviewer checks passed for committed changes; the remote Canary build compiled and typechecked. These are separate from the remaining live acceptance checks above. No claim of permanent zero failures, production-wide repair, or new-OS launch readiness is made.

## Final live acceptance on ee121072f

- Supported owner update installed Omarchy installer SHA256 `32d81ebf19745597d18938c1789169aa7a43fd7e84e06aede05fc8475b024a76`. The layout service is active and actual `debug:vfr` is false; all three Omarchy services are active.
- Unassisted HQ → QHD → 4K → HQ works. Host monitor and decoded browser canvas independently match QHD 2752×1338 and optional 4K 4080×2008, scale 1. No diagnostic wake command was run during those acceptance switches. HQ remains the selected default.
- A subsequent ordinary Omarchy page reload connected in 5.5 seconds with no Prepare/Update/Retry action. An earlier post-update secure setup displayed 4.7 seconds. These are observed secure-setup timings, not new-VM provisioning times or physical input latency.
- Final database reads confirm Windows, Ubuntu, and Omarchy running/running with null operation slots. The extra Ubuntu VMs are stopped/stopped, preserving disks. No Hetzner capacity purchase or additional spending occurred.
- Pinned Selkies already implements hidden-tab STOP, foreground START, bounded decoder queues and keyframe recovery. The cadence repair supports its compositor restart path, but actual minimize/return latency is not confirmed. Browser automation renders screenshots and delivers mouse input, but keyboard delivery is unreliable; direct stream navigation is blocked and was not bypassed. **Remaining acceptance requires real typing, cursor motion, and tab-away/return testing.**
- New-VM first boot, fresh Windows/Omarchy image provisioning, installed native-app acceptance, and Windows evaluation-license longevity are not established by this repair. The existing Windows instance is an evaluation installation; do not describe it as perpetual licensed hosting. The old QGA hang's underlying cause remains unproven despite successful recovery.

Continue from the deployed release and the same existing VMs. Do not rebuild them or release operation leases with fabricated evidence. Do not repeat the obsolete geometry/wake hypotheses above: the final unassisted switches are the acceptance result.

### Latest Windows cold-open failure — unresolved

After the working RDP session, a fresh catalog-style `?tab=desktop&open=fast` entry automatically launched but returned “Windows RDP inspection could not be confirmed. Try opening it again.” No Prepare action was attempted. Direct QGA ping subsequently succeeds (host UTC 13:32:18). This invalidates any claim that Windows cold opening is consistently fixed. Diagnose its exact inspection path; do not repeat blind reboots or mistake the earlier successful session for current complete acceptance.

Subsequent cloud operations inspection returned HTTP 503 with `guest_remote_exit`, while the actual generated host wrapper and descriptor parser passed directly in about 25 seconds. That generic code can represent any remote host script failure; it does not prove a guest-agent failure. Reviewed diagnostic commit `f98303b15` was deployed as `dpl_2N4anAFcaunvwG767CnVYbB9VqCY` and aliased to Canary. Its Windows-only allowlist preserves bounded PowerShell/CLIXML and QGA failure tokens without exposing raw stderr or authorizing more preparation. The next cloud inspection returned HTTP 200, and automatic catalog-style opening reached the real Guacamole/RDP desktop with no Prepare/Retry action. **This diagnostic-only change cannot establish that the intermittent root cause was repaired.**

Windows individual key events successfully typed `example.com` and loaded the HTTPS Example Domain page in Edge. Bulk automated text insertion dropped characters, so the individual-key test supersedes earlier keyboard-unconfirmed entries for Windows only. Omarchy's embedded Selkies keyboard test remains blocked by browser-control focus/target errors; no browser safety barrier was bypassed. Actual Selkies typing, cursor-motion latency and background-return acceptance remain unconfirmed.

Reviewed diagnostic `168594663` was subsequently deployed as `dpl_9MBYTty8jaxkoueKGYjVoxRfTQnY`. It identifies fixed host phases and bounded remote exit codes, without relaxing complete-descriptor or authority checks. An actual SSH2 invocation passed in 28.126 seconds; this does not establish the intermittent cloud failure's cause.

A single ephemeral, read-only PowerShell profile measured firewall conflict scanning at 16.833 seconds of 20.530 seconds of guest work. Reviewed `3655d9a14` checks RDP-capable ports/protocols before expensive service/application associations. Its actual Windows PowerShell equivalence fixture passed all 13 cases with identical old/new conflict sets. Four focused suites passed 107 tests; the optional local PowerShell test is skipped because `pwsh` is absent, but that same fixture was separately executed successfully on Windows. The actual strict SSH2 inspector then passed in 18.739 seconds, 33% faster than the earlier 28.126 seconds. No caching, TTL, firewall, authority or preparation permissions were changed. Browser acceptance of this release is recorded separately after deployment.

One subsequent read-only bulk-filter probe found 91 candidate rules and 532 port filters. All 91 relevant rule Name/InstanceID associations matched exactly once and matched the existing associated filter's InstanceID, protocol and local port; none were missing or duplicated. Bulk retrieval took 105 ms, versus 13,516 ms for the per-rule lookups plus comparison pipelines. This latter timing includes comparison overhead, not isolated command latency. The proposed optimization uses fresh data inside one inspection, not cached admission. Microsoft documents the [GetAll port-filter lookup and one-to-one rule/filter relationship](https://learn.microsoft.com/en-us/powershell/module/netsecurity/get-netfirewallportfilter?view=windowsserver2025-ps); its default policy store remains the same PersistentStore used by the existing scan.

Reviewed bulk-map commit `64f627909` passed the actual Windows 13-case equivalence fixture and rejected all four missing/duplicate/cross-ID/name-inconsistent associations. Its full encoded command then failed QGA dispatch before PowerShell: actual command length 33,319 exceeded the Windows 32,767-character limit. Explicit Win32 ERROR206 was not surfaced, so do not claim the historical intermittent failure had this same cause. Canary was restored to known-working `3655d9a14` (`dpl_6UHdUDJamxxmf1BuwXr9qS72HxpU`), and normal automatic Windows opening again rendered the real desktop without Prepare/Retry.

Reviewed `e5cfa89e1` repairs the command-size violation through the existing VMID-scoped QGA stdin helper. A small fixed bootstrap accepts bounded strict base64/UTF-8 trusted source and executes it without writing guest files or stripping program comments. The Windows inspection source/revision remains `44e5fda661fe285764be7083bf0d92a43c9e12837ecec49996b5467839265b86`; certificate and private gateway source remain unchanged. The authentic SSH2 inspector passed in 11.053 seconds, versus 28.126 seconds initially. Windows argv is 1,243 characters and stdin payload 16,616 characters, bounded to 32,768. Three focused suites passed 88 tests with one optional local PowerShell skip; lint and reviewer checks pass. Public browser acceptance follows the final deployment, not the local timing.

On ready, aliased `e5cfa89e1`, a new catalog-style Windows entry automatically handed off to the real Guacamole/RDP desktop, preserving the existing Edge Example Domain page. No Prepare/Retry action occurred. The 11.053-second figure above is actual inspector timing, not a precisely measured full browser-open duration.

### Omarchy lease expiry / foreground return — follow-up in progress

A fresh browser surface revealed “session ended” and manual Reconnect. Cached screenshots/accessibility output must not be counted as durability acceptance. Session `00000000-0000-4000-8000-000000001155` was created at 13:27:26Z, renewed 13 times, last issued at 14:04:07Z and expired at 14:08:07.792Z. Guest WebSocket cleanup at 14:08:07.793Z matches lease expiry exactly. The database records released input and `computer_stopping`; the actual VM remains running/running with no operation. The capability was freshly observed at 14:07:36Z, expires at 14:15:36Z and has the same generation, so generation rotation and missing Wayland renewal admission are not established causes. SQL resets `issued_at` on renewal; it is not the original creation time.

The prior broker did not log failed renewal HTTP statuses, and available guest/Vercel logs cannot recover that prior failure. A corrected query of the actual `/api/remote-desktop/sessions/renew` path also returned no logs; this does not prove no outbound attempt. Do not fabricate its cause or call it a background socket drop.

Bounded frontend repair `2cfe634ca` is pushed and deployed as `dpl_BoUBcyizAgeaWfKu74mFz5NsEVLm`: one fresh owner-authorized connection on return from a previously connected hidden episode, explicitly disabling automatic Prepare; no old lease reuse, indefinite retry or unauthorized ownership takeover. A delayed iframe disconnect after the visibility event is covered, without consuming the reconnect attempt during a refresh-only return. Forty-four focused UI tests, lint and fresh review pass. The actual Omarchy owner flow opens without Prepare (latest displayed secure setup 12.7 seconds); switching Terminal → Desktop preserves its connected session. This is not live proof of ended-hidden recovery. The in-app browser currently reports all test documents as visible, and keyboard delivery to the actual visible `videoStream` surface fails with an input-target mismatch; hidden-tab timing and Selkies interaction acceptance remain unverified.

Diagnostic-only broker change `78c7c317f` records fixed token-free renewal attempt/outcome events with allowlisted HTTP/transport/invalid-receipt/late-result classes; no IDs, URLs, tokens, bodies or raw errors. Twenty-four broker tests, 44 capability/preparation tests, lint and fresh review pass. Current deterministic broker hash is `c3413d5ec63128ea31ada705292914ed86d8e2a8fac1ff5dac526cbd648cd5fe`, bundle `2598e9f417d088884c4864b37e4024f8ede88018e7e8f4c9286e6fb27ba6d9d5`; historical manifests are unchanged. Canary deployment is ready and aliased; the supported Omarchy Update runtime installed the exact hash and returned to a connected stream displaying 5.9 seconds. An immediate service check during the restart reported failed; the completed follow-up confirms all three services active and both containers healthy/running. This instrumentation does not repair the unknown underlying renewal failure. Renewal acceptance is in progress.

## Current-session continuation — Chrome and cross-device handoff

- The post-update Omarchy session `00000000-0000-4000-8000-000000001156` completed two logged HTTP-200 renewals before `connection_closed` at 14:47:16Z. It did not reproduce the earlier lease-expiry failure; this short check is not sustained durability proof.
- Windows normal cold opening on `78c7c317f` failed with the generic inspection-unconfirmed message. Following authorized ops inspection returned an authentic descriptor at 14:46:08Z; direct SSH2 passed with 1,023 stdout and 10,276 stderr bytes (11,299 combined, below the 16KiB cap). Neither later pass establishes the cause of that earlier failure; output overflow is only an unproven hypothesis.
- `007301da7` adds strict fixed-envelope failure-reason server logging only; 32 route tests, lint and fresh review pass. It is deployed ready/aliased as `dpl_6P7WNyLi8seHn4s7FLaEAUkR9kTA`. The subsequent Chrome normal Windows owner flow automatically reached the actual RDP desktop showing the retained Example Domain page. This is an opening pass, not proof that intermittent inspection failure is repaired.
- Chrome is now available. A fresh normal Ubuntu Chrome opening reproduced the cross-device controller conflict: the previous browser still held input. `6642fe365` implements an explicit initial-open same-owner Hivra browser/Selkies handoff using only fully validated session rows and existing owner-scoped revocation. New authority remains behind real guest input-release acknowledgment and the serialized SQL fence. Foreground automatic recovery and subsequent retries do not displace active controllers. No guest logoff/restart or multiplayer behavior is introduced. 117 focused tests and lint pass; fresh review corrected a numeric expiry validation gap. Deployment and actual handoff acceptance are pending.
- Windows prepared Canary is an externally installed evaluation fixture, not a shipped fresh licensed-image pipeline. Source contains no unattended Windows image/autologon builder; ordinary Windows provisioning remains gated. Omarchy relies on its already-running owner Hyprland session, so blanket Linux graphical-autologin removal would break it. The prior live Windows AutoAdminLogon change alone was not source protection for new preparation; bounded preparation/inspection boot-policy protection is being verified separately.

### Combined release and actual Chrome acceptance

- `fea114264` persists Windows boot policy in preparation and read-only inspection: only a registry `REG_SZ` AutoAdminLogon of `0`/`1` is accepted; missing means disabled. Preparation conditionally changes `1` to `0` and verifies it before further changes. Unknown registry types/values fail closed. No password/username registry values are read and no session is logged off. Ninety-four focused checks pass; all nine mocked production-block boot-policy cases also passed inside actual Windows VM2098 with registry calls shadowed, no real registry writes, and exit code 0.
- The first combined deployment failed TypeScript because a parameterized test widened `computerKind`. Test-only `85f090bae` preserves literal types; full TypeScript and 117 focused tests pass. The replacement deployment above completed and is aliased; the failed build never replaced Canary.
- On that deployed release, Ubuntu's normal Chrome reload rendered Selkies with 6.5-second displayed secure setup, replacing the prior browser's same-owner controller without manual Prepare/Retry. A second normal Chrome entry then rendered the same running desktop in 10.4 seconds. The previous Chrome controller visibly ended instead of continuing to send input. This proves the normal same-owner handoff path; it is not multiplayer or proof of zero future networking failures.
- Chrome Omarchy normal typing and Return executed `echo HIVRA_OK` and visibly returned `HIVRA_OK`. Earlier shortcut attempts delivered escape fragments to the old pending terminal line; they do not establish Ctrl/Super shortcut acceptance. Ordinary typing supersedes the prior blanket keyboard-unavailable statement, but modifier handling, duplicate-cursor diagnosis and genuine hidden/return acceptance remain pending.

### Broker watchdog repair — source verified, rollout pending

- Due renewals now dispatch on every watchdog tick before bounded session authorization work. One authorization is shared across each session's sockets; batches of four prevent serial socket checks from starving later renewal dispatch. This fixes deterministic scheduling starvation, not a proven explanation of the historical expiry incident.
- Finalization synchronously closes owned media sockets before input resumption/map deletion. A delayed upgrade authorization cannot admit media after finalization, broker closure, replacement or actual expiry. Authority, disconnect grace, real expiry and guest acknowledgment contracts remain unchanged.
- Fresh independent review passed. All 31 broker tests, 44 capability/preparation tests, TypeScript and pin-file lint passed. Final broker hash is `3c8814c5662fafa313a1cb6371ce34a5e313c0696d57fc9a3d830243e1ed585c`; derived Ubuntu bundle is `9beeb61195795feb78f86196f5a9b059d1b6828adcbd81f1c00d409642b68cfe`. Historical manifests were not changed. These bytes are not yet installed live.
- Native Mac UI is locked; browser-only Chrome remains usable. Normal existing-account Canary reauthentication and catalog Omarchy opening rendered Selkies without Prepare/Retry, displaying 11.8-second secure setup. This is on the prior deployed release, not acceptance of the new broker.

### Watchdog rollout acceptance

- Source `6d48a8495` is pushed and deployed ready/aliased as `dpl_7DNgqWnWodLEaxKJ5G56aZi2m9oT` (`hermesos-canary-mrfd435pw-ashneil12s-projects.vercel.app`). This supersedes the pending rollout status above. Supported owner Omarchy Update desktop and Ubuntu normal automatic runtime repair installed the exact broker `3c8814c5662fafa313a1cb6371ce34a5e313c0696d57fc9a3d830243e1ed585c`, verified independently through read-only QGA SHA256.
- Omarchy reopened at displayed 6.2-second setup; ordinary browser input `echo HIVRA_BROKER_OK` visibly returned `HIVRA_BROKER_OK`. Its new broker logs contain a successful HTTP-200 renewal. This is one observed renewal, not sustained durability proof.
- Ubuntu's opening including runtime repair took 83.4 seconds; its subsequent normal warm reload connected in displayed 7.2 seconds without Update/Prepare/Retry. Do not present the maintenance-inclusive duration as warm launch time.
- Windows' supported fast-desktop button on this release reached the actual retained Edge Example Domain desktop over Guacamole/RDP. No VM restart, logoff, disk deletion or spend occurred. Only one Ubuntu remains running.
- QHD profile switching changed actual decoded frames to Omarchy 2934×1256 and Ubuntu 3024×1200 (both readyState 4), without diagnostic wake input. Both profiles were restored to HQ and all three desktop tabs retained for use.
- Latest Omarchy stream logs confirm FullFrame Streaming (Turbo), software x264 and llvmpipe compositor. A single recent 30-minute log snapshot contains no backpressure/stall warnings; absence does not prove healthy motion latency. Disabling Turbo is an existing supported candidate, not a proven fix. No speculative FPS, VFR, cursor or capacity changes were made. Native Mac UI remains locked; physical cursor/motion/foreground verification still requires an unlocked session.

### Unlocked native Chrome checks

- After the user unlocked the Mac, native Chrome input executed `echo HIVRA_UNLOCKED` and visibly returned `HIVRA_UNLOCKED`. Two native clicks yielded browser input-to-changed-frame readings of 65ms and 18ms, two measurements and no stalls. These are not physical input-to-photon or sustained motion measurements.
- Native `sleep 30` → Control-C → `echo HIVRA_CANCEL_OK` visibly showed cancellation and `HIVRA_CANCEL_OK`; subsequent read-only process inspection found no remaining sleep process. This supersedes earlier unreliable Control-C attempts, not every possible keyboard shortcut.
- A genuine native Chrome New tab interval followed by returning to the original Omarchy tab retained session `00000000-0000-4000-8000-000000001157`. Native `echo HIVRA_TAB_RETURN` visibly returned its exact output without Prepare/Retry. Recent current broker logs show three HTTP-200 renewals; this is not acceptance beyond the historical 40-minute expiry interval.
- Current native screenshots show one pointer; the user's duplicate cursor was not reproduced. Read-only exact installed frontend source reproduction identified a separate asynchronous race: `updateServerCursor()` awaits image decoding in canvas mode, then switching to native mode during the await can be overwritten by the stale completion. It can leave native mode true with canvas visible and CSS cursor hidden; current live DOM does not match that state. No speculative cursor change has been installed.

### Omarchy acceleration and actual boot-cycle defect

- NODE-B has a GT 710 (GK208B), nouveau driver and `/dev/dri/renderD128`, with no VM host-PCI assignments for this device. VM2099 originally used plain `vga: virtio`, explaining why no VirGL renderer was available. Required host GL/EGL libraries were absent; an isolated diskless QEMU probe failed loading `libEGL.so.1`.
- Authorized NODE-B graphics-library installation added ten packages and patch-updated `libgbm1`/`mesa-libgallium` to `25.0.7-2+deb13u1`, with no removals, host reboot or unrelated package upgrades. The repeated isolated QEMU VirGL/EGL probe initialized and survived until its five-second timeout; the probe process was cleaned up. This is graphics initialization evidence, not motion latency evidence.
- Supported Hivra Stop → stopped observation → `qm set 2099 --vga virtio-gl` → supported Start preserved the non-VGA disk/network/size/boot configuration hash `26ddd20e25b26d1de9faea2fc1e1711822e6b88b83384074a1bef9ab1c9cd275`. Hyprland actually initialized `Renderer: virgl (NV106)`. Ubuntu1108 and Windows2098 remained running. Omarchy open apps were interrupted; disks were not rebuilt or deleted. No spending occurred.
- First normal Desktop opening failed because all three enabled desktop services were inactive with no boot entries. Actual boot journal records an ordering cycle and deletion of the layout start job: generated layout/Selkies waited for `graphical.target` while belonging to `multi-user.target`, whose implicit service ordering comes before graphical. This is a reproduced source defect, not a GPU initialization failure.
- Reviewed source `1c54feee8` removes that invalid generated graphical ordering and preserves existing Docker/layout/stream/broker dependencies, restart behavior, mounts and security. Installer hash is `d77a7b94fd9cee57d665250a6b67a60bba3b4303237cd1afcd09a884c8e10202`; derived capability pin matches. Four Python checks pass with the optional local systemd test skipped on Mac; the conditional test evaluates actual generated expressions and verifies an original-cycle negative control on systemd environments. Forty-four capability/preparation tests and TypeScript pass. Rollout, actual installed systemd verification and repeat boot acceptance are pending.
- The same source adds explicitly validated `--graphics virtio-gl` to the existing lab launcher, with GL/EGL/render-node preflight before resources are claimed. Default `virtio` is retained for unqualified hosts. Invalid, missing or duplicate options fail closed; 25 focused checks, lint and independent review pass. This does not qualify all future hosts, change general provisioning, install libraries automatically, or make fresh licensed Windows images available.

### Boot-order rollout and inherited lifecycle lock repair

- `046a21764` deployed ready/aliased as `dpl_CqS5NErHEDmQzZ2h9kQ3uJdKLdjS`. Normal Omarchy Desktop opening automatically repaired the approved old runtime without a Prepare/Retry action and rendered at displayed 33.8-second setup including that repair. Read-only QGA verifies the exact installer `d77a7b94fd9cee57d665250a6b67a60bba3b4303237cd1afcd09a884c8e10202`, all three services active, and actual installed `systemd-analyze verify` exit zero with no warnings. This supersedes the pending installer rollout above; repeat real boot acceptance follows separately.
- The subsequent normal owner Stop failed after 60 seconds, retaining operation `00000000-0000-4000-8000-000000001158`; actual database error was `prepared computer lifecycle is busy`. NODE-B `lslocks` identifies KVM PID2497424 holding `/run/lock/hivra-prepared-2099.lock`. `onboot` remained 1 and no shutdown jobs ran: the Start shell's FD8 lock was inherited by daemonized QEMU and persisted for the entire VM lifetime. This is a separate reproduced lifecycle defect, not a stream startup failure.
- Authorized `qm shutdown 2099 --timeout 60` completed cleanly, and `qm status` verified stopped with the leaked lock absent. `onboot=0` and existing startup parameters were persisted to honor the already-requested stopped state. Only then did the existing exact owner/operation/desired-state completion RPC return true. No blind operation reset, forged receipt, forced stop, disk deletion or other-VM restart occurred; Ubuntu1108 and Windows2098 remained running.
- Reviewed `24eb37b4c` closes FD8 only in both `qm start` children, preserving the parent shell's serialization lock. Sixteen focused tests and scoped lint pass, including real inherited-FD/flock fixtures proving parent lock retention and release while daemon children remain alive. Actual Bash receipt tests pass unchanged; an initial escaping suspicion was disproved and no receipt change was made. The release is ready/aliased above; normal Start/Stop and post-boot desktop acceptance follow separately.
- Native Windows RDP input now changes Edge's address to `https://example.org` and loads the real Example Domain page. This supersedes the earlier keyboard-unconfirmed statement; it is not a sustained Windows latency benchmark.
- On `24eb37b4c`, normal Hivra Start returned the running state with `onboot=1`, persisted `vga=virtio-gl`, and no VM-held prepared lifecycle lock. All three actual desktop units started automatically; the current boot contains no ordering-cycle/deleted-start-job failure. Normal Desktop opening connected in displayed 11.7 seconds without Prepare/Retry or manual service starts. Native Super-Return launched a terminal, and native `echo HIVRA_BOOT_OK` visibly returned `HIVRA_BOOT_OK`.
- Native QHD selection independently produced decoded video 2934×1256, readyState4, without wake-up input; HQ was restored. Two post-acceleration guest samples show Hyprland 24.2/40.8% CPU and 75.3/77.2% idle, versus the earlier approximately 230% compositor load/28–29% idle. Samples are not matched sustained-motion benchmarks; the first native input-to-changed-frame observation was 24ms, not physical input-to-photon proof.
- Subsequent normal Stop completed through Hivra with actual stopped VM and no VM-held lock; normal Start completed again. All three primary database rows are running/running with null operations/errors. This passes the FD8 lifecycle repair, not every subsequent desktop opening.

### Hidden startup, cursor capture and pre-boot capability follow-up

- A second boot's Desktop return immediately showed the prior stream-opening timeout although all three units were active. One explicit Try again rendered in 5.1 seconds without preparation. Source traces the defect: retained `desktopOpened` remounts the component hidden on Manage after Start, but its mount effect unconditionally connects and begins a 30-second invisible handoff timeout. Reviewed `e874ac11d` gates the first connection on active Desktop while preserving already-connected hidden retention. Forty-six tests, lint and full TypeScript pass; deployment acceptance is pending.
- The current capture source identifies a narrower duplicate-pointer cause than the earlier speculative Hyprland software-cursor path. Installed Selkies `set_native_cursor_rendering(enabled)` composes the host pointer into video; the client can enable it during trackpad initialization even with the CSS pointer visible. Actual current Virtual-1 at 2200×942 reports hardwareCursorsInUse=true, contradicting a blanket software-cursor explanation. Reviewed `867ae8353` delegates that control to upstream with false, leaving cursor metadata and input callbacks intact. Nine derived Python tests and 44 capability/preparation checks pass; installer pin is `ffa5410152d7a0506d52ca2be5b1e88b6567215a044bf11c900725af13b2b05a`. Installation and native visual acceptance are pending; no compositor invisible/hardware-cursor setting was changed.
- Session `00000000-0000-4000-8000-000000001159` created18:09:40Z received two HTTP200 renewals that the broker rejected as invalid receipts, followed by HTTP409. Database expiry18:11:53Z, last renewal18:11:11Z/count2, and revocation18:11:51Z/capability_rotated are exact evidence. The expiry corresponds to an older pre-reboot capability; the later genuine fresh-boot inspection rotated generation. Host/guest UTC differ by only about one second. The response shape matches; broker extension/expiry checks remain correct and were not weakened.
- Reviewed `d02c57e61` invalidates the exact owner's desktop capability under each claimed prepared Windows/Omarchy and normal Proxmox Ubuntu power operation, before host dispatch. Invalidation failure blocks dispatch and releases only the known-not-dispatched operation. This forces a later boot to prove a fresh capability rather than reusing the previous boot's proof; it does not bypass release acknowledgment, change TTLs, or claim the independent provider-engine branch is repaired. Sixty route checks, lint and TypeScript pass. The combined release above is deployed; live boot/cursor acceptance follows separately.

### Combined follow-up rollout acceptance

- `599427328` is deployed ready/aliased as `dpl_8b53Xfcyv5HK15SPb5DwAkJBWbAd`. Normal Desktop opening automatically installed cursor policy `ffa5410152d7a0506d52ca2be5b1e88b6567215a044bf11c900725af13b2b05a` and rendered with 36.2-second displayed setup including maintenance. Read-only QGA confirms that exact installer and all three active units.
- Native Chrome's Super-Return opened a terminal. Typing after the terminal was ready executed `echo HIVRA_CURSOR_OK` and visibly returned its exact output. Native screenshots with the physical pointer outside the stream and browser screenshots show no retained pointer inside video. The browser CSS pointer/input path remains usable. A window-opening click measured158ms and subsequent click27ms, two samples/no stalls; these do not establish sustained motion or physical latency. Automation screenshots can display separate tool/OS pointer indicators outside the stream; they are not evidence of a second encoded pointer.
- Normal Stop revoked the old capability at18:28:36Z, and session `00000000-0000-4000-8000-000000001160` reached released/computer_stopping. Hivra Stop then completed and normal Start succeeded with no VM-held lifecycle lock. While Manage remained selected after Start, the latest session stayed the revoked18:26:24Z row: no hidden session was created. All three stream services started automatically on this new boot. Desktop-after-wait acceptance is recorded next.
- After more than one minute on Manage, selecting Desktop opened Omarchy in displayed 11.4 seconds without Prepare/Retry. Native Super-Return and `echo HIVRA_FINAL_BOOT_OK` visibly returned the exact output on this final boot. The new session `00000000-0000-4000-8000-000000001161`, created18:31:17Z, received two successful broker renewals; at18:37:21Z its expiry extended to18:41:21Z and the capability was freshly observed18:37:25Z. This exceeds the old boot's stale capability interval, but not the historical 40-minute failure interval.
- Normal Windows Stop completed with actual VM2098 stopped; normal Start completed and the supported fast-desktop button reached the real Windows desktop without Prepare/Retry. Native keyboard input opened Windows Search and typed Microsoft Edge. This boot acceptance does not constitute a sustained latency benchmark.
- Normal Ubuntu Stop completed with VM1108 stopped; normal Start returned running. Desktop selection automatically repaired and rendered, without Prepare/Retry, but took88.9 seconds. Investigation identifies an actual regression: the normal Ubuntu inspector reuses its installation-time capability generation after reboot, while the new power fence revokes that generation. SQL correctly rejects re-recording the same revoked generation, forcing an unnecessary installation. A boot-bound read-only generation fix is in progress; this delay is not accepted as fast startup.
- At18:43:25Z, the same Omarchy session had four successful renewals, expiry18:47:25Z, active input and no revocation: more than twelve minutes since opening and beyond the initial eight-minute capability interval. This is bounded renewal continuity acceptance, not proof beyond forty minutes or under every network interruption.
- Windows Edge was opened from the retained desktop, and native Control-L plus `https://example.org` loaded the real Example Domain page on this final boot. This proves native keyboard and public internet access after the power cycle; no browser permission was granted or user login erased for the test.
- Reviewed `02eea86f5` binds normal Proxmox Ubuntu's observed generation to the installed UUID and validated Linux boot UUID using UUIDv5. It stays stable within a boot, changes across boots, and fails closed for unavailable/malformed boot identity. The independent provider inspection branch remains unchanged. Thirty-nine focused tests, scoped lint and full TypeScript pass, with independent review PASS. No installer/bundle bytes or capability guards were relaxed. Deployment and repeat Ubuntu boot acceptance follow below.
- Final `02eea86f5` release is ready/aliased above. With Ubuntu1108 stopped through the normal UI, the page reloaded the new release, normal Start completed, and selecting Desktop opened the actual full-panel desktop in displayed6.9 seconds, without runtime repair, Prepare or Retry. Read-only QGA after boot reports boot UUID `00000000-0000-4000-8000-000000001162`; installed capability.json timestamp remains18:39:44Z before this boot, and broker SHA256 remains unchanged. The accepted observed capability is fresh18:50:48Z with boot-derived UUID `00000000-0000-4000-8000-000000001163`. This proves no runtime reinstallation for the tested boot; the displayed6.9 seconds is desktop-open time after the VM returned running, not total provisioning time.
- At18:49:29Z, Omarchy session `00000000-0000-4000-8000-000000001161` still has active input, no revocation and six successful renewals with expiry18:53:29Z, eighteen minutes after opening. Native return from Windows then `echo HIVRA_LONG_RETURN_OK` visibly returned its exact output without Prepare/Retry or a new session.
- Final database state for all three primary targets is running/running, with null operations/errors. VM2099,2098,1108 are left running; extra Ubuntu1109/1112 remain stopped with disks retained. All three browser desktop tabs are retained. No capacity was purchased (£0 spend), user files/logins were preserved, and only the explicitly tested computers were power-cycled. Fresh-VM provisioning, licensed Windows image delivery, native-app build acceptance, forty-minute continuity and interrupted-network recovery remain distinct unverified work, not claims of permanent completion.
- Final Ubuntu streamed Control-Alt-T opened Konsole, and ordinary stream keyboard input `echo HIVRA_UBUNTU_BOOT_OK` visibly returned its exact output. Two browser input-to-changed-frame click observations were56/53ms with no stalls, not sustained/physical latency evidence. An incidental System Settings window was closed without changing settings; the working Konsole is retained for use.

### Explicit reconnect handoff follow-up

- The user's reconnect screenshot reports `controller_conflict`, not a boot failure. The retained Chrome Ubuntu test session `00000000-0000-4000-8000-000000001164` is still active with seven renewals at19:12:27Z, confirming that the test connection held the lease. Retaining a VM is not a reason to retain an input-owning test stream.
- Reviewed `5aa886ece` exposes **Take over here** on a terminal controller conflict. Normal opening/retry/foreground recovery no longer implicitly requests owner handoff; the explicit button requests it once, then waits behind the existing guest release-acknowledgment fence. Existing owner/computer/generation/transport validation is preserved. Two changed UI files, 119 focused UI/broker/API checks, scoped lint and independent review PASS. Rollout and two-tab acceptance follow below.
- The previous Omarchy session `00000000-0000-4000-8000-000000001161` remains active with fourteen renewals through19:13:46Z, over42 minutes since18:31:17Z opening. Ordinary streamed `echo HIVRA_40_MIN_OK` visibly returns its exact output at approximately19:16Z. This supersedes the earlier forty-minute-interval gap for this observed session, not every network interruption. The owned Omarchy browser was then navigated to Manage to release its input while keeping the VM running.
- Release `5aa886ece` is ready/aliased as `dpl_Bbrk6G2ibL8RXpiWuHcUzFQF8DzD` (`hermesos-canary-4wl6pj1qq-ashneil12s-projects.vercel.app`), with remote compilation and TypeScript passed. A second authenticated Chrome tab showed the real controller-conflict error and **Take over here** while the first session remained active. Pressing the button revoked the old session at19:18:49Z; its input reached released before new session `00000000-0000-4000-8000-000000001165` was issued19:18:51Z. The new desktop rendered in displayed13.3 seconds with the existing Konsole intact; ordinary input `echo HIVRA_TAKEOVER_OK` visibly returned its exact output. No manual old-tab cancellation, VM restart, Prepare or runtime installation was required.
- Cleanup closed the new owned takeover test tab and navigated the prior Ubuntu/Omarchy/Windows test tabs to Manage instead of retaining active streams. VMs and user terminal/apps remain intact; no account or permission changes, disk deletion or spending. The user must refresh an already-loaded old dashboard to receive the new button; this shared Selkies reconnect acceptance covers Ubuntu, not Windows RDP arbitration or multiplayer.

### Windows inline follow-up, 19:40Z

- User clarification: Esc did not exit because the previous Windows action navigated the main browser to Guacamole using `window.location.assign`; this was not browser fullscreen. Reviewed `53448719d` replaces that navigation with an authenticated, exact-origin-validated in-memory iframe. Fullscreen is optional and preserves the frame. Computer-switch guards prevent stale preparation/handoff responses from changing another target. Thirty-five regression tests and scoped lint pass.
- Canary deployment `dpl_DaGEnaGxhvEPm8mgTKVvJqtePCiq` is READY and aliased to canary.hermesos.cloud, with remote compilation and TypeScript passed. Chrome's normal Desktop/Open fast desktop flow displayed the actual Windows Edge desktop inline, retaining Hivra navigation and the same computer URL. Manage then Desktop returned to the retained desktop without another launch or navigation. Clicking the streamed map changed the remote page, proving pointer interaction beyond iframe onLoad.
- Automation's fullscreen request was rejected; the inline desktop stayed usable and displayed the explicit rejection message. Native Chrome capture failed twice, so trusted-click fullscreen/Esc acceptance remains unverified. Do not claim it passed. Remote keyboard shortcut acceptance was inconclusive and is not counted as passed.
- Omarchy double-cursor report remains unresolved: installed pixelflux 2.1.0 external capture logs cursor=consumer, and live nested DOM uses a native CSS image cursor with hidden canvases and no second cursor DOM node. The earlier force-false policy did not establish full visual acceptance for the user's report. No invisible-cursor or speculative compositor setting was applied.

### Windows aspect-ratio follow-up

- The affected public in-app browser iframe measured 816×617, while the decoded Windows canvas was 1920×1088 and rendered 816×462.4. This confirms roughly 155 pixels of unused vertical space, not a loading overlay. The encrypted gateway token forced a 16:9 profile resolution.
- Reviewed `162fdca64` sends visible panel dimensions through the owner-bound handoff API and scales the actual Windows desktop to that aspect within the selected quality bounds. Strict geometry validation rejects invalid/extreme dimensions before guest inspection or token exchange. No stretching, cropping, automatic reconnection or iframe replacement was introduced. Eighty-nine tests across three focused suites, scoped lint and independent review passed.
- Later panel resizing relies on the existing Guacamole display-update path; manual Reconnect Windows remeasures the panel if it does not resize automatically. A hidden/zero-size panel retains the backwards-compatible preset fallback. Live acceptance is recorded separately after rollout; source checks alone are not visual acceptance.
- First rollout removed the large bars but revealed negotiated resolution rounding: canvas1472×1088 rendered840.95×621.57 in an816×617 panel, clipping a narrow edge. Reviewed `fa2e22645` aligns adaptive requests to the observed64×16 blocks within the selected quality bounds (HQ1408×1072). Ninety-three focused tests passed. Deployment `dpl_49DLsn3DxhbeBDxo6Jh5TkKqwsWJ` is READY and aliased to Canary; remote compilation/TypeScript passed. No guest reboot, firewall/auth change, unrelated desktop mutation or spending.
- That second visual check matched the requested width1408 but negotiated height1088 instead of1072, proving that height also rounds to64 pixels. The final correction fits the panel inside a64×64-aligned quality bounding box (HQ1344×1024), with minimum aligned dimension256. The earlier64×16 assumption is superseded; the second rollout is not counted as a no-cropping pass.
- Final `a4e2896b4` deployment `dpl_AZKjpKR4wFmeABApeaD9PoSSujCh` is READY/aliased. Normal refresh/Open fast desktop in the affected public in-app browser negotiated exactly1344×1024, rendered809.8125×617 in the816×617 panel. The entire frame fits with about3 pixels of side padding and no vertical letterbox or cropped edges. Actual Edge and the Windows taskbar remain visible; existing user apps/content survived. This verifies the reported inline geometry, not every future viewport change or other operating systems. Testing reused the user's existing tab; no extra test stream remains, and the running desktop was left available rather than logging the user out.

### Earlier native Chrome evidence continued

- Native Google Chrome app selection (not merely creating agent tabs) selected the real Omarchy tab, then New tab, then returned. The existing session `00000000-0000-4000-8000-000000001166` remained connected. `echo HIVRA_NATIVE` and `echo HIVRA_RETURN_OK` both visibly returned their exact output. Browser input-to-frame readings were 12 and 24ms with no recorded stalls; these are browser telemetry, not physical latency measurements or sustained motion benchmarks. This short tab-away/return pass does not prove ended-hidden recovery or explain the earlier long-session expiry. A separate Control-C attempt did not reliably cancel the pending harmless command; do not count modifier acceptance as passed.
- Current Windows catalog-style entry on `85f090bae` automatically reached the actual Guacamole/RDP desktop with the retained Example Domain page and no Prepare/Retry action. The intermittent inspection root cause remains unproven despite this opening pass.
- Current Omarchy broker logs contain three recent `renew_attempt` / `renew_succeeded` HTTP-200 pairs. The short interval does not establish long-session durability.
- Cursor diagnosis: installed Selkies external Wayland capture logs `cursor=consumer`. `enable_cursors=false` does not govern the actual external cursor callback. A stale client canvas is possible in the installed frontend, but the current DOM disproves that explanation for this session: fixed cursor canvas is hidden and `overlayInput` uses a native CSS image cursor. Native-app screenshots show a second pointer, while agent screenshots/rendering differ; video-baked software cursor versus automation/OS pointer artifacts remains unresolved. Actual Hyprland has `no_hardware_cursors=2` (Auto), `invisible=false`. Do not set `cursor:invisible=true` as a shortcut: matching compositor source resets the cursor image buffer and can destroy remote cursor-shape metadata. No speculative cursor configuration change was made.
- Read-only performance check: exact Hyprland startup renderer field reports `llvmpipe (LLVM 22.1.8, 256 bits)`. Two guest samples show Hyprland at 227.9/231.2% CPU (about 2.3 of four cores), versus Selkies 48.5/44.6%. Guest idle is 28–29%, with no swap/I/O pressure or meaningful steal, about 5GB available RAM. Compositor software rendering dominates CPU, not another agent workload. Configured stream is 60FPS / 25Mbps with queue limit four; actual encoded FPS and queue occupancy are not confirmed. Metrics returned unauthenticated 401 and no credentials were obtained. Do not equate CPU samples with proof of encoder saturation or the complete cause of perceived lag. The existing VFR=false cadence fixes independently verified native-resize deadlock; do not revert it without preserving unassisted profile switching and foreground recovery.
